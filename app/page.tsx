"use client";
import {useState} from "react";import {AnimatePresence,motion} from "framer-motion";import eventData from "@/data/event.json";import {OpeningExperience} from "@/components/Opening/OpeningExperience";import {InvitationContent} from "@/components/Invitation/InvitationContent";import {AmbientButterflies} from "@/components/Opening/AmbientButterflies";
export default function Home(){const[opened,setOpened]=useState(false);return <main className="relative min-h-screen overflow-hidden px-4"><AmbientButterflies/><AnimatePresence mode="wait">{!opened?<motion.div key="opening" exit={{opacity:0,scale:1.05,filter:"blur(8px)"}} transition={{duration:.8,ease:"easeInOut"}}><OpeningExperience event={eventData.event} onComplete={()=>setOpened(true)}/></motion.div>:<motion.div key="content" initial={{opacity:0,y:24,scale:.985}} animate={{opacity:1,y:0,scale:1}} transition={{duration:.9,ease:"easeOut"}}><InvitationContent config={eventData}/></motion.div>}</AnimatePresence></main>}
