"use client";import {useState} from "react";type RSVP={options:string[];successTitle:string;successMessage:string;successFooter:string};export function RSVPForm({rsvp}:{rsvp:RSVP}){const[submitted,setSubmitted]=useState(false);function handleSubmit(formData:FormData){const data={name:String(formData.get("name")||"").trim(),attendance:String(formData.get("attendance")||""),timestamp:new Date().toISOString()};console.log("RSVP provisional:",data);setSubmitted(true)}return <section className="panel">{!submitted?<><p className="kicker">Confirma tu asistencia</p><form action={handleSubmit} className="mt-5 text-left"><label className="mb-2 block font-medium" htmlFor="name">Nombre</label><input className="min-h-[52px] w-full rounded-[18px] border border-[rgba(200,168,78,.30)] bg-[rgba(255,255,255,.82)] px-4 py-3 outline-none focus:border-coral focus:ring-4 focus:ring-coral/10" id="name" name="name" type="text" placeholder="Escribe tu nombre" autoComplete="name" required/><fieldset className="mt-5 border-0 p-0"><legend className="mb-2 font-medium">¿Nos acompañarás?</legend>{rsvp.options.map(option=><label key={option} className="my-3 flex min-h-[52px] items-center gap-3 rounded-[18px] border border-[rgba(200,168,78,.20)] bg-white/60 p-4"><input type="radio" name="attendance" value={option} required/><span>{option}</span></label>)}</fieldset><button className="primary-button mt-4 w-full" type="submit">Confirmar</button></form></>:<div className="py-6 text-center"><div className="mx-auto mb-5 grid h-[74px] w-[74px] place-items-center rounded-full bg-gradient-to-br from-coral to-[#b64b50] font-serif font-bold text-white shadow-[0_14px_24px_rgba(99,45,52,.22)]">J&C</div><h3 className="font-serif text-3xl text-[#553D42]">{rsvp.successTitle}</h3><p className="mx-auto mt-3 max-w-[340px] text-muted">{rsvp.successMessage}</p><p className="mt-3 font-serif text-xl text-[#8b6d20]">{rsvp.successFooter} 🦋</p><button className="secondary-button mt-6" onClick={()=>window.scrollTo({top:0,behavior:"smooth"})}>Volver a la invitación</button></div>}</section>}
